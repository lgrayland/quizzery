<%= form_for(@given_answer) do |f| %>
  <% if @given_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@given_answer.errors.count, "error") %> prohibited this given_answer from being saved:</h2>

      <ul>
      <% @given_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :participation_id %><br>
    <%= f.number_field :participation_id %>
  </div>
  <div class="field">
    <%= f.label :quiz_question_id %><br>
    <%= f.number_field :quiz_question_id %>
  </div>
  <div class="field">
    <%= f.label :answer_choice_id %><br>
    <%= f.number_field :answer_choice_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
