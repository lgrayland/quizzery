<%= form_for(@answer_choice) do |f| %>
  <% if @answer_choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_choice.errors.count, "error") %> prohibited this answer_choice from being saved:</h2>

      <ul>
      <% @answer_choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="field">
    <%= f.label :correct %><br>
    <%= f.check_box :correct %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
